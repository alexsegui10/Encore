# Encore - Health Check Configuration
# Este archivo documenta los health checks que deberías agregar en producción

# Para agregar health checks a docker-compose.yml:

# booking_client:
#   healthcheck:
#     test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4000/health"]
#     interval: 30s
#     timeout: 10s
#     retries: 3
#     start_period: 40s

# admin_server:
#   healthcheck:
#     test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
#     interval: 30s
#     timeout: 10s
#     retries: 3
#     start_period: 40s

# frontend:
#   healthcheck:
#     test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80"]
#     interval: 30s
#     timeout: 10s
#     retries: 3

# mongo:
#   healthcheck:
#     test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
#     interval: 10s
#     timeout: 5s
#     retries: 5

# mysql:
#   healthcheck:
#     test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p$MYSQL_ROOT_PASSWORD"]
#     interval: 10s
#     timeout: 5s
#     retries: 5
